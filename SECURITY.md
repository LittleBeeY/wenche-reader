# 安全政策

## 支持版本

当前仅维护最新的 `1.x` 版本。安全修复会进入最新补丁版本。

## 报告漏洞

请不要在公开 Issue 中披露尚未修复的漏洞。请通过 GitHub 仓库的 **Security > Report a vulnerability** 私密报告，并包含：

- 受影响版本和运行环境
- 可复现步骤或最小样例
- 可能影响
- 已知缓解方式（如有）

项目维护者会尽快确认报告，在修复发布后再协调公开细节。请勿提交真实 API Key、私人文档或数据库文件。

## 安全边界

文澈阅读 V1.0 是本地单用户应用，默认只监听 `127.0.0.1`。它没有账号、用户隔离和公网防护，不应直接部署到公网。

应用导出的文澈备份不包含 `.env` 或 API Key，但包含原始文章、阅读标注和 AI 回答。请将备份视为私人文档，不要附到公开 Issue、提交到 Git 或放入公开网盘。
