# 版本记录

本项目遵循语义化版本。

## [1.2.0] - 2026-08-11

### 功能

- 源码启动（Web 版）AI 设置支持直接使用启动进程环境变量 `AI_API_KEY`（可搭配 `AI_PROVIDER`/`AI_API_BASE`/`AI_MODEL`）：`.env` 未保存 Key 时自动回退环境变量，仅当前会话、不回显、不落盘；设置对话框显示 Key 来源提示，留空保存也不会把环境变量 Key 写入 `.env`。
- 环境变量 Key 自动识别常见别名变量名：`AI_API_KEY` 缺失时按当前 provider 优先匹配 `OPENAI_API_KEY`/`DEEPSEEK_API_KEY`/`MOONSHOT_API_KEY`/`ZHIPUAI_API_KEY`/`DASHSCOPE_API_KEY`/`ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`GOOGLE_API_KEY`，桌面版与源码版行为一致，设置对话框显示实际来源变量名（不落盘、不回显）。
- 环境变量 Key 可多选：同时存在多个 Key 变量时，AI 设置对话框显示下拉框让用户选择用哪个（默认“自动按接口匹配”）；桌面版切换即生效，源码版保存后生效，均为当前会话、不落盘；顺带修复桌面版保存设置时把环境变量 Key 写入密钥文件的问题。
- AI 状态栏提示 Key 来源：桌面版已保存 Key 生效、但环境变量也存在 Key 时，主界面状态栏显示“当前用已保存 Key，检测到环境变量 XXX，点击切换”入口，可一键切换到环境变量 Key（仅当前会话、不落盘）。
- 今日精选改为每天自动生成：后台调度器在每天首次运行时自动生成当天精选，无需再手动点「立即生成」；没有候选正文的条目不再保存为空精选，避免出现“今日精选横幅 + 空列表”的误导状态；当天无候选时会持续重试直到生成成功。
- 对抗性审查修复：环境变量 Key 名称加入白名单校验（仅接受已知别名），服务端复用共享的 `sanitizeEnvValue`；Web 端会话级别名 Key 写回其来源变量名（不再提升为 `AI_API_KEY` 丢失来源）；桌面端保存设置后同步 `savedAiProvider`；桌面端已保存 provider 为 mock（未配置）时环境变量 provider 生效（修复别名匹配与配置合并）；桌面 e2e 启动清理宿主 AI 环境变量，避免别名变量污染断言。

### 桌面版修复

- 修复桌面版保存 AI 设置超时失败：main 补齐 `settings-write` 持久化与确认回执，Key 继续经 safeStorage 加密保存，不再依赖遗漏的消息处理。
- AI 设置支持环境变量 Key（`AI_API_KEY` 等，仅当前会话、不落盘、不回显），设置对话框显示 Key 来源并可显式选用。
- 「关于与更新」新增「卸载应用」入口：确认后执行 Squirrel `Update.exe --uninstall`，开发模式拒绝执行；卸载仍保留本地阅读数据。
- Squirrel 品牌化：卸载列表、开始菜单与桌面快捷方式显示「文澈阅读」，包 ID 与可执行文件名保持英文。
- 修复小窗口（非全屏）下 AI 解析面板底部被视口裁切的问题：默认面板高度限制在视口内。
- 修复 RSS 文章阅读模式下 AI 面板被渲染到正文下方（761–1439px 窗口时完全不可见）的问题：面板恢复为悬浮层，收起后的小方块按钮与本地文档一致。
- 「关于与更新」改为共享对话框：本地文档侧栏与 RSS 文章「更多」菜单都可打开，版本、更新、日志与卸载入口在两种视图下一致。
- 设置统一收敛为单一模块：`public/settingsHub.js` + 一个「设置」对话框（AI 接口 / 资讯 / 数据备份与恢复 / 关于与更新 四个区段），本地侧栏、RSS 页脚、RSS 文章「更多」菜单与 AI 状态栏全部打开同一实例，不再按视图维护各自的设置入口。
- 「数据」区段新增存储占用管理与缓存清理：显示 data/uploads/资讯图片缓存/备份/浏览缓存大小，可一键清理 `cache/rss-images` 与浏览缓存。
- 数据根支持迁移到自定义位置：「设置 → 数据 → 更改位置」把 data/uploads/cache/backups 搬到其他磁盘（含 SQLite 路径事务改写、跨盘复制、失败回滚与 `data-location.json` 指针），`config/secrets/logs/session` 留在固定引导根；顺带修复备份清理函数因缺少 `readdir` 导入而静默失效的问题。
- 检查更新在未配置更新源时明确提示「更新未启用（未配置更新源，正式发布后可用）」，不再笼统显示「当前无法检查更新」。
- 修复 AI 接口设置启动期点击无响应：顶层 await 不再阻塞事件监听器注册，启动数据改在模块末尾统一加载。
- AI 接口设置区段按需自加载：从侧栏「设置 → 数据」或标签切换进入也能填充接口列表，不再空白、无法选择。
- AI 设置弹窗改为内容就绪后再显示；对话框移除 `backdrop-filter` 并常驻合成层、使用不透明背景，消除软件渲染下的黑帧闪烁；标签重新激活时保留未保存输入。
- 从第一性原理修复 AI 设置弹窗整个框闪一下：此前 `transform: translateZ(0)` 对 `display:none` 的 `<dialog>` 无效（元素未参与渲染，不会创建合成层），`showModal()` 时从 `display:none` 跳到 `display:block` 并新建合成表面，软渲染下首帧内容未光栅化上屏即闪烁。现覆盖 UA 样式让设置对话框从加载起就 `display:block + opacity:0` 常驻渲染树与合成层，配合 `inert` 阻断未打开时的交互与聚焦，`showModal()` 只改透明度复用已有表面，不再闪帧。
- 打包忽略 `release/`，修复旧安装包被卷进新包导致体积从 172MB 膨胀到 515MB 的问题。
- 文档明确桌面版启动方式：`WencheReader-Setup.exe` 是一次性安装器，日常从桌面/开始菜单「文澈阅读」快捷方式启动，避免用户重复运行安装器以为程序打不开。
- 开发启动抑制 Windows 未处理异常弹窗：`desktop:dev` 注入 `ELECTRON_DEFAULT_ERROR_MODE=1`，软件渲染下的 0x80000003 断点异常不再弹框。

## [1.1.0] - 2026-08-06

### 功能

- AI 接口配置重构为「预设 + 传输层适配器」：`AI_PROVIDER` 支持厂商预设（`deepseek`/`openai`/`kimi`/`zhipu`/`qwen`/`ollama`/`anthropic`/`gemini`）与原生传输层（`openai-compatible`/`anthropic`/`gemini`），新增 Anthropic Messages 与 Google Gemini 原生协议适配器；`config:ai` 脚本改为接口选择菜单并按接口类型检查连接。
- 行为变化：`AI_PROVIDER` 拼写错误时启动直接报错（不再静默退回 Mock）；本地 Ollama 预设无需 API Key；`AI_API_BASE`/`AI_MODEL` 仍可覆盖预设默认值；AI 历史记录的 provider 显示预设名（如 `deepseek`）而非传输层类型。
- AI 接口改在应用内配置：点击 AI 面板顶部状态栏打开设置对话框，选择接口、填写 Key、测试连接并保存；保存后立即生效无需重启，`/api/ai/settings` 读写 `.env`（不回显 Key、留空保持原 Key、保留注释与其他配置），`/api/ai/settings/test` 按接口类型检查连接。命令行 `config:ai` 保留为可选兜底。
- 新增“资讯”一级模式（RSS 2.0 / Atom 1.0）：粘贴 Feed 或网站地址自动探测订阅，订阅源分组、重命名、优先级、暂停与失败重试。
- OPML 批量导入（先预览并区分新增、重复、可恢复与无效条目）与导出。
- 资讯收件箱：未读/全部/已读筛选、收藏、稍后读、不感兴趣、搜索、紧凑/摘要/卡片三种密度与批量状态操作；全部标记已读需二次确认。
- 后台调度：每分钟检查到期订阅源，ETag/Last-Modified 条件请求，抖动与指数退避（最长 24 小时），单源失败不影响其他来源。
- 今日精选：每天生成有限重点内容，附可解释推荐原因；同一来源默认不超过两条，支持重新生成，当天内保持稳定。
- AI 初评：仅分析最近 48 小时候选，输出结构化摘要、主题与质量信号；失败回退确定性排序，可全局关闭并设每日预算。
- 打开资讯即生成隐藏阅读快照，完整复用分页、划词解析、批注、沉淀与 Markdown 导出；可保存到本地文档；仅摘要来源支持按需提取全文。
- 抓取安全：仅 http/https，拒绝本机与内网地址（含 IPv4 变体与 IPv4-mapped IPv6），连接层防 DNS 重绑定，逐跳重定向校验，大小与解压限制，禁止 XML 外部实体。
- 备份升级 V2：包含订阅、分组、资讯状态、偏好与简报，兼容 V1 恢复；普通资讯正文缓存可用 `includeRssCache=1` 选入。
- 增加持久化划词高亮、批注、分页书签和沉淀视图。
- AI 回答可一键沉淀，并可编辑标题、补充笔记或移出沉淀。
- 支持按当前文章或全部文章导出 Markdown 阅读沉淀。
- 支持从页面下载并恢复包含文章原文件和结构化数据的本地备份，备份排除 API Key。
- 增加阅读字号、内容宽度和行距调节，并在浏览器本地保存设置。
- 改进 DOCX 标题、引用、题注、下划线、表格属性和内嵌图片的格式保留。
- DOCX 使用 docx-preview 增加 Word 页面高保真模式，保留页眉页脚、颜色、页面尺寸和表格视觉样式。
- 修复阅读版标题双重放大，以及字号设置只影响正文、不影响标题和表格的问题。
- 修正 DOCX 超长版式段被误计为单页的问题，按正文块重新装入独立纸页，避免多页内容拼成连续长纸。
- 为重新分页后的每张纸复制页眉页脚，并补齐 docx-preview 未计算的动态页码字段。
- 页码明确区分当前归档中的“文档”和当前文档内的“页”。
- 高亮保存和删除改为原地更新，避免跳回页首；点击已高亮文字可直接取消。
- 旧 HTML 首次打开时自动适应阅读区宽度，并保留原有布局比例。
- 重构三栏阅读工作区的视觉层级、控件密度、文档列表滚动和 AI 沉淀视图，并明确“导出本篇沉淀”的作用范围。
- 阅读工具栏按容器宽度自动换行，窄屏 Word 纸页默认适应可用宽度。
- 增加明亮、护眼和夜间阅读主题，主题在浏览器本地持久化。
- 增加沉浸阅读模式，可一键收起工作区控件并通过 `Esc` 退出；划词解析会自动恢复 AI 面板。
- AI 回答改为流式显示，完成后再写入历史，并支持从回答的页码或段落依据定位回原文。
- 修复新版前端连接旧版服务时把 JSON 回答误判为未完成流的问题，并统一使用中文错误提示。
- 沉浸阅读底部保留阅读设置与浮动翻页条，支持调整字号和主题，以及上一页、下一页和当前页码显示。
- 精简左侧资料库，默认聚焦导入、搜索、归档筛选和文章列表，批量整理与备份改为按需展开，并为后续 RSS 来源预留独立入口。
- 统一左侧资料库用词为“文档”和“文件夹”，移除重复的当前文档标题，并重新分组批量移动、删除、新建文件夹和备份恢复操作。
- 修正资讯智能/最旧排序的游标分页，避免超过首屏后漏项；手动刷新现在会检查全部未暂停来源，并报告新增与正文更新数量。
- Feed 中同一条目的标题或正文变化会增量更新，同时保留已读、收藏等状态；网页全文提取不会被后续 Feed 正文覆盖。
- 全文提取会在没有阅读资产时安全重建当前快照；已有标注或 AI 记录时保护旧快照并明确提示。
- 完善订阅源级刷新间隔、正文获取方式和 AI 初评开关；重新订阅会恢复暂停来源，新增来源采用已保存的默认刷新间隔。
- 中等宽度下 AI 面板改为覆盖资讯列表，正文不再被四栏挤压；窄屏改为资讯导航、列表、正文之间的单任务返回路径。

### 测试与稳定性

- 增加 RSS/Atom 解析、OPML、SSRF 防护（回环、私网、地址变体、连接层重绑定）、排序与今日精选的单元测试。
- 增加订阅发现、ETag 304、失败退避、条目状态、快照幂等、取消订阅保留沉淀、备份恢复兼容的 API 测试。
- 增加资讯完整闭环（订阅、列表、深读 AI、收藏、简报）与 OPML 的 Chrome、Edge、Firefox 端到端测试。
- 增加标注、AI 沉淀、Markdown 导出、备份恢复和旧数据库迁移测试。
- 增加 Chrome、Edge、Firefox 端到端测试及复杂 HTML 表格样本。
- 增加 DOCX 常用格式、阅读设置持久化和窄屏弹层回归测试。
- 增加 DOCX 原文件访问边界、跨浏览器高保真分页、划词 AI、搜索和整页缩放测试。
- 增加长 DOCX 独立纸页、正文不越过页脚、逐页页码、HTML 宽布局自适应、高亮位置保持和点击取消的跨浏览器回归测试。
- 增加窄屏 Word 纸页宽度适配和界面控件溢出检查。
- 增加 AI 流式协议、引用解析、主题持久化、沉浸模式和原文定位的单元、接口及跨浏览器测试。
- 修复恢复完成后清理旧文件失败可能影响新文件的边界。
- 增加资料库默认简洁状态、批量整理展开状态和 RSS 入口状态的端到端回归检查。
- 增加手动强制刷新、Feed 内容更新、智能/最旧排序完整分页、提取全文快照保护、RSS 字段迁移和窄屏单任务导航回归测试。

## 1.0.0 - 2026-07-22

### 功能

- 批量导入 TXT、Markdown、HTML、PDF、DOCX 和 EPUB。
- 命名归档、自然排序、搜索、连续分页、阅读进度和批量删除。
- 划词直接解析、深入解析、自定义提问和 AI 历史记录。
- 安全 Markdown 渲染、HTML 保真阅读和同归档相对链接跳转。
- 可折叠文档栏和 AI 面板，划词操作自动展开 AI 面板。
- DeepSeek 与其他 OpenAI-compatible 模型配置脚本。

### 安全与稳定性

- 默认仅监听 `127.0.0.1`。
- 增加上传大小、批次数量、文件签名和 AI 输入限制。
- 增加 EPUB 解压大小、条目数量和压缩率防护。
- 增加 CSP 等浏览器安全响应头。
- 升级受影响依赖并补充解析、安全和 API 回归测试。
- 升级 PDF 解析器并增加 PDF、DOCX、EPUB 实际解析测试。
- AI 配置脚本隐藏密钥输入并验证 provider 模型列表。
